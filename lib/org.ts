// The chapter this deployment runs for. Meal Move is single-chapter, so a
// volunteer's "organization" is a fixed, read-only identity (shown on /profile),
// not a per-user field. Change this one constant to rebrand the chapter.
export const CHAPTER_NAME = "Campus Food Rescue";
